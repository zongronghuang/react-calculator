import {
  clearCurrentInputHelper,
  negateLastNumberHelper,
  keyinHelper,
  normalizeOperatorsHelper,
} from "./input.helpers";

describe("[Input Helpers]", () => {
  test("first", () => {
    expect(3 + 2).toBe(5);
  });
});
