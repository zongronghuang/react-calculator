import { useEffect } from "react";

const useKeyboardInput = ({
  keyinHandler,
  clearCurrentInput,
  getComputedValue,
}) => {
  useEffect(() => {
    return () => {};
  }, []);
};

export default useKeyboardInput;
